Lancer le docker du projet : docker-compose -f docker-compose.yml up -d 
Stopper le docker du projet : docker-compose -f docker-compose.yml down -v

Lancer le docker de tests : docker-compose -f docker-compose.test.yml up -d 
Stopper le docker de tests : docker-compose -f docker-compose.test.yml down -v